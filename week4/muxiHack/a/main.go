package main

import "fmt"

func GetAllKeys(nums []int) [][]int {
	for i := 0 ; i <= re(n)-1; i++ {
		nums = append(nums, n)
}

func main() {
	n := 0
	fmt.Scanf("%d", &n)
	key := make([]int, n)
	res := GetAllKeys(key)
	fmt.Println(res)
}
